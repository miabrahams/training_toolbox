version: "v1.10"
name: "First graph with known lineage - SDXL Notes still present"
comment: Added group nodes
roles:
  checkpoint:
    node_id: 173
    node_type: "CheckpointLoaderSimple"
    inputs:
      ckpt_name: text
  positive:
    node_id: 185
    node_type: "DPCombinatorialGenerator"
    inputs:
      text: text
  negative:
    node_id: 7
    node_type: "CLIPTextEncode"
    inputs:
      text: optional_text
  ksampler:
    node_id: 70
    node_type: "KSampler"
    inputs:
      steps: int
      cfg: float
      sampler_name: text
      scheduler: text
      seed: int
  latent:
    node_id: 5
    node_type: "EmptyLatentImage"
    inputs:
      width: int
      height: int

outputs:
  positive_prompt: [positive, text]
  negative_prompt: [negative, text]
  checkpoint: [checkpoint, ckpt_name]
  steps: [ksampler, steps]
  cfg: [ksampler, cfg]
  sampler_name: [ksampler, sampler_name]
  scheduler: [ksampler, scheduler]
  width: [latent, width]
  height: [latent, height]