version: "v0.83"
name: "Old Comfyui Win"
comment: Different ckpt node
roles:
  checkpoint:
    node_id: 75
    node_type: "Checkpoint Loader"
    inputs:
      ckpt_name: text
  positive:
    node_id: 89
    node_type: "DPCombinatorialGenerator"
    inputs:
      text: text
  negative:
    node_id: 4
    node_type: "CLIPTextEncode"
    inputs:
      text: optional_text
  sampler:
    node_id: 54
    node_type: "KSampler"
    inputs:
      steps: int
      cfg: float
      sampler_name: text
      scheduler: text
  latent:
    node_id: 6
    node_type: "EmptyLatentImage"
    inputs:
      width: int
      height: int

outputs:
  positive_prompt: [positive, text]
  negative_prompt: [negative, text]
  checkpoint: [checkpoint, ckpt_name]
  steps: [sampler, steps]
  cfg: [sampler, cfg]
  sampler_name: [sampler, sampler_name]
  scheduler: [sampler, scheduler]
  width: [latent, width]
  height: [latent, height]
